# Install torch separately using the cu128 index; it installs matching Triton 3.6.0.
torch==2.10.0
transformers==4.57.6
accelerate==1.12.0
bitsandbytes==0.49.1
# vLLM must use a separate environment to avoid replacing this torch/Triton pair.
